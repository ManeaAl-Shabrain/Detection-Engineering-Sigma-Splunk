# exports/

Raw working files from the lab - not the polished deliverables. Drop here as you go:

- Splunk search results exported to CSV/JSON
- `savedsearches.conf` snippets for your alerts
- Atomic Red Team output logs
- any scratch queries you're iterating on

Screenshots live in each phase folder (e.g. [`../phase-B-detections/`](../phase-B-detections/phase-B.md));
the final rule set stays in [`../../rules/`](../../rules/).
